
<h1>The Users Lists </h1>




<% @user.each do |user|%>

<div class=container>
<div class="row d-flex justify-content-center">
  <div class="col-md-8">
<div class="card text-center  mb-5">
<div class="card-header">
  <%= link_to  user.username, customers_path(user) %>
</div>
  <div class="card-body">
    <h5 class="card-title">korean learning portal</h5>
    <p class="card-text"><%= user.articles.count %> no. of articles created by <%= user.username %></p>
    
  </div>
  <div class="card-footer text-muted">
    <%= user.created_at %>
  </div>
</div>
</div>
</div>
</div>
</div>
<% end%>


  <div class="flickr_pagination d-flex justify-content-center m-5">
    <%= will_paginate @user, :container => false %>
  </div>
  